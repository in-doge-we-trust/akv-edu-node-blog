/**
 * @type {import('prettier').Config}
 */
const config = {
  printWidth: 80,
  endOfLine: 'lf',
  useTabs: false,
  tabWidth: 2,
  singleQuote: true,
  arrowParens: 'always',
  semi: true,
  trailingComma: 'all',
  bracketSpacing: true,
};

module.exports = config;
